pipeline {
    agent any
    environment {
        PROJECT_NAME = "Employee Portal"
    }
    
    stages {
        stage('ProjectName') {
            steps {
                echo "${PROJECT_NAME}"
            }
        }
    }
}
