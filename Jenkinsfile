pipeline {
    agent any
    environment {
        PROJECT_NAME = "Employee Portal"
    }
    
    stages {
        stage('ProjectName') {
            steps {
                echo "Project : ${PROJECT_NAME}"
                echo "BUILD_NUMBER : ${BUILD_NUMBER}"
            }
        }
    }
}
